[project]
name = "neraium"
version = "0.1.0"
description = "Neraium structural evidence engine"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "fastapi",
    "httpx",
    "pytest",
    "python-multipart"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
